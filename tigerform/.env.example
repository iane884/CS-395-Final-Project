# Copy to .env and fill in. The app/pipeline work without this (deterministic
# template feedback is used as a fallback), but setting it enables natural,
# Claude-phrased coaching.
ANTHROPIC_API_KEY=

# Optional: override the default model used for feedback phrasing.
TIGERFORM_FEEDBACK_MODEL=claude-opus-4-8
